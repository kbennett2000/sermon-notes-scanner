// Pinned settings-android.gradle for ORT Android AAR builds.
// Keep in sync with makeacopy gradle/libs.versions.toml [agp]
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'onnxruntime'
rootProject.buildFileName = 'build-android.gradle'
