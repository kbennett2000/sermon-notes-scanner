package de.schliweb.makeacopy;

import androidx.appcompat.app.AppCompatActivity;
import dagger.hilt.android.AndroidEntryPoint;

/**
 * Empty {@link AndroidEntryPoint} activity used exclusively by instrumented tests so that Hilt
 * fragments can be launched via {@code FragmentScenario} without crashing.
 */
@AndroidEntryPoint
public class HiltTestActivity extends AppCompatActivity {}
